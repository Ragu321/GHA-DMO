name: output values
on: push
jobs:
  output-demo:
    runs-on: ubuntu-latest
    steps:
      - name: step one 
        id: steps1
        run: |
         echo "version= 1.1" >> $GITHUB_OUTPUT
      - name: step2 TWO
        run: echo " the version is  ${{ steps.steps1.outputs.version }}"
         
        
